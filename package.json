{
  "name": "jam-wonderstatus-ios",
  "version": "1.0.0",
  "description": "JAM_WonderStatus for iOS – create and share property statuses",
  "main": "index.html",
  "scripts": {
    "build": "mkdir -p www && cp -r assets/* www/ 2>/dev/null || true && cp index.html www/ && cp styles.css www/ && cp renderer.js www/",
    "start": "npx cap open ios",
    "sync": "npx cap sync",
    "copy": "npx cap copy"
  },
  "dependencies": {
    "@capacitor/core": "^5.0.0",
    "@capacitor/filesystem": "^5.0.0",
    "@capacitor/share": "^5.0.0",
    "@capacitor/device": "^5.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^5.0.0",
    "@capacitor/ios": "^5.0.0"
  }
}